const urlEndpoint = {
    extendUrl: '/*',
}

const rgbColor = {
    black: 'rgb(0, 0, 0)',
}

const timeout = {
    payment: 120,
    general: 30,
    toastMessage: 10,
}

module.exports = {
    timeout,
    urlEndpoint,
    rgbColor,
}
